number = [1, 5, 3, 5]
alphabet = ["a", "b", "c", "d"]

# Merge
merge = number + alphabet
print(merge)

# Replication
apping = ["A", "P", "P", "I", "N", "G"]
replication = apping * 2
print(replication)
